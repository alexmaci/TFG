import { NuevaEmpresaPipe } from './nueva-empresa.pipe';

describe('NuevaEmpresaPipe', () => {
  it('create an instance', () => {
    const pipe = new NuevaEmpresaPipe();
    expect(pipe).toBeTruthy();
  });
});
